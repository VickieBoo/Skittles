namespace SkittlesPower {
    public static class Tolerances {
        public static double Transparency = 255; // minimum alpha (transparency) allowed (0 full transparency allowed, 255 solid colors only)
        public static double Similarity = 0.20; // how similar a color can be until it's considered a different color
    }
}